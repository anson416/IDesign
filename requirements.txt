# Dependency specs live in pyproject.toml. Install one of:
#
#   pip install -e .                      # core: LLM scene-graph generation only
#   pip install -e ".[render]"            # + --render / --render-all (Blender bpy 5.1.2)
#   pip install -e ".[retrieve]"          # + --retrieve (OpenShape/CLIP/Objaverse assets)
#   pip install -e ".[viz]"               # + utils.get_visualization (matplotlib/scipy)
#   pip install -e ".[dev]"               # + pytest
#   pip install -e ".[all]"               # everything
#
# Requires Python 3.13 (bpy 5.1.2 is cp313-only; ag2 provides the `autogen`
# namespace on py3.13). See pyproject.toml for the full spec.
